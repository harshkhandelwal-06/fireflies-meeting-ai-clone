2.4.0 BUILD FIX

Fixed Next.js TypeScript error in frontend/app/page.tsx:
- Home no longer references setCreateMode/setCreate directly.
- Upload action is passed into Home via an upload callback.

Version bumped to 2.4.0 in package.json and build-windows.bat.

Static verification performed:
- node --check desktop/main.cjs: PASS
- python -m py_compile backend/app/main.py backend/launcher.py: PASS
- Confirmed no setCreateMode reference remains inside Home component.

Note: full Next.js production build could not be completed in this Linux environment because the dependency tree was unavailable/incomplete after npm install interruption. Run build-windows.bat on Windows; the previously reported setCreateMode error is removed.
